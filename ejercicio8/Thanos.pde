class Thanos {
  //ENEMIGO PRINCIPAL  
    // Campos - variables (propiedades)
      // Posición
      // Movimiento
      // Velocidad

  // Constructor - metodo o funcion inicial
    // Inicializar posicion, movimiento y velocidad

  // Metodos - funciones (acciones)
    // dibujarThanos() // Mostrar al enemigo

  //movimientoThanos()
    // irArriba() // Mover hacia arriba

    // irAbajo() // Mover hacia abajo

  //velocidadThanos() //Cada vez que un proyectil de IronMan lo golpee, este aumenta la velocidad de su movimiento

  //dispararGuante() // El enemigo dispara con su guante de manera horizontal y de forma azar (cada cierto tiempo)
}
